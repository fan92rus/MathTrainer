# Шаблон PRD для нового типа заданий MathTrainer

**Версия:** 1.0
**Дата:** 9 февраля 2026
**Назначение:** Обеспечить соответствие новых упражнений существующей архитектуре MathTrainer

---

## Цель этого документа

Этот шаблон гарантирует, что **новое упражнение будет использовать существующие компоненты и паттерны** MathTrainer, избегая дублирования кода и архитектурных несоответствий.

---

## 🔴 Обязательные разделы PRD

### 1. Existing Patterns Reference (Ссылка на паттерны)

**ЭТО САМЫЙ ВАЖНЫЙ РАЗДЕЛ!** Без него разработчик создаст дубликаты компонентов.

```markdown
## 11. Existing Patterns Reference (Ссылка на паттерны)

### 11.1 Обязательные общие компоненты

| Компонент | Путь | Обязательные props | События | Пример использования |
|-----------|------|-------------------|---------|---------------------|
| GameOver | `src/components/common/GameOver.vue` | correctAnswers, totalAnswers, score | @restart, @exit | После завершения игры |
| CoinAnimation | `src/components/common/CoinAnimation.vue` | amount, showText?, duration? | @animationEnd | При awardCoins |
| ScoreDisplay | `src/components/common/ScoreDisplay.vue` | currentScore, totalScore, currentQuestion, totalQuestions | - | Отображение прогресса |
| ProgressBar | `src/components/common/ProgressBar.vue` | progressPercent | - | Визуальный прогресс |
| StarRating | `src/components/common/StarRating.vue` | score | - | Рейтинг по звёздам |
| AnswerOptions | `src/components/common/AnswerOptions.vue` | options[], correctIndex, answered, selectedIndex | @answer-selected | Выбор ответа |
| CurrencyDisplay | `src/components/player/CurrencyDisplay.vue` | (нет, использует playerStore) | - | Отображение валюты |

### 11.2 Обязательная структура DOM View-компонента

```
app-container
├── CoinAnimation (условный рендеринг)
└── game-container
    ├── .header
    │   ├── button.back-button (← Назад)
    │   ├── span.level-indicator (Уровень X)
    │   └── CurrencyDisplay
    ├── game-container-inner (если !gameOver)
    │   ├── h1.title (Название упражнения)
    │   ├── ScoreDisplay
    │   ├── [Problem Display - ваш компонент]
    │   ├── ProgressBar
    │   ├── StarRating
    │   └── AnswerOptions
    └── GameOver (если gameOver)
```

### 11.3 Переиспользование структуры компонентов

> 🎯 **КРИТИЧЕСКИ ВАЖНО:** НЕ создавайте компоненты с нуля! Переиспользуйте существующие структуры.

**Правило:** Скопируйте файл-пример и адаптируйте под своё упражнение.

```
1. Найдите похожее существующее упражнение
2. Скопируйте файл в новую директорию
3. Переименуйте и адаптируйте под своё упражнение
4. НЕ переписывайте структуру с нуля!
```

**Примеры для копирования по типу упражнения:**

| Тип упражнения | Файл-пример | Что скопировать |
|----------------|-------------|-----------------|
| С визуализацией | `ColumnSubtractionView.vue` | View, Display компоненты |
| С уровнями | `MultiplicationView.vue` | Логику уровней |
| Простое | `DecompositionView.vue` | Базовую структуру |

**Что НЕ менять при копировании:**
- Структуру `app-container` → `game-container` → `game-container-inner`
- Импорт общих компонентов (GameOver, ScoreDisplay, и т.д.)
- Порядок компонентов в template
- Использование `useGameLogic`, `useCoins`

### 11.4 Reference Implementations (Примеры для копирования)

> ⚠️ **Внимание:** Номера строк могут устареть при рефакторинге. Проверьте актуальность перед использованием.

**Архитектура View:** `src/views/DecompositionView.vue`
- Header структура: строки 12-20
- ScoreDisplay использование: строки 23-28
- AnswerOptions использование: строки 48-54
- GameOver использование: строки 58-65
- CoinAnimation использование: строки 4-8

**Генератор задач:** `src/utils/math/decomposition/index.ts`
- Функция generateXXXProblem: строки 50-120
- Функция generateWrongOptions: строки 20-45

**Стили:** `src/views/DecompositionView.vue` строки 214-262
- .back-button: строки 215-231
- .level-indicator: добавить по аналогии с CurrencyDisplay

### 11.5 Обязательные Composables и Store

| Composable/Store | Путь | Назначение |
|------------------|------|------------|
| useGameLogic | `src/composables/useGameLogic.ts` | Управление состоянием игры |
| useCoins | `src/composables/useCoins.ts` | Анимация и выдача монет |
| useScoresStore | `src/store/scores.ts` | Хранение очков упражнения |
| usePlayerStore | `src/store/player.ts` | Валюта и достижения |

### 11.6 Требования к Options API

**Правило выбора синтаксиса:**

| Тип компонента | Синтаксис | Пример |
|----------------|-----------|---------|
| **View-компоненты** (Learning/Diagnostic/Training) | Options API | `ColumnSubtractionView.vue` |
| **Display-компоненты** (маленькие, presentaion) | `<script setup>` | `ColumnDisplay.vue` |
| **Common-компоненты** (переиспользуемые) | `<script setup>` | `ScoreDisplay.vue` |

**View-компоненты ДОЛЖНЫ использовать Options API.**

❌ **НЕПРАВИЛЬНО для View:**
```vue
<script setup lang="ts">
import { ref } from 'vue'
const count = ref(0)
</script>
```

✅ **ПРАВИЛЬНО для View:**
```vue
<script lang="ts">
export default {
  name: 'SomeView',
  components: {
    ScoreDisplay,
    ProgressBar,
    // ...
  },
  setup() {
    const count = ref(0)
    return { count }
  }
}
</script>
```

**Display-компоненты МОГУТ использовать `<script setup>`.**

✅ **ПРАВИЛЬНО для Display:**
```vue
<script setup lang="ts">
import { ref } from 'vue'

export interface Props {
  value: number
}

const props = defineProps<Props>()
</script>
```
```

### 2. Design System & CSS Requirements

**ПРАВИЛО:** НЕ дублируйте существующие стили из `src/assets/css/main.css`!

#### Глобальные классы (НЕ переопределять в scoped styles):

```css
/* ИСПОЛЬЗОВАТЬ ИЗ main.css - НЕ копировать в компонент! */
.back-button { /* строки 326-357 */ }
.level-indicator { /* строка 66-74 */ }
.app-container { /* строки 18-25 */ }
.game-container { /* строки 27-37 */ }
.title { /* строки 55-64 */ }
```

#### Стратегия работы со scoped styles:

**✅ ДЕЛАЙ ТАК:**
```vue
<style scoped>
/* Только уникальные стили для компонента */
.problem-container { /* ваш специфический стиль */ }
.hint-btn { /* ваш специфический стиль */ }
/* НЕ дублировать .back-button, .level-indicator и т.д. */
</style>
```

**❌ НЕ ДЕЛАЙ ТАК:**
```vue
<style scoped>
/* Дублирование глобальных стилей! */
.back-button {
  background: linear-gradient(135deg, #667eea, #764ba2);
  /* Это УЖЕ ЕСТЬ в main.css! */
}
</style>
```

#### Цвета и градиенты (для новых компонентов)

```markdown
## NFR-007: Design System

### Цвета и градиенты (НЕ использовать CSS переменные)

**Primary gradient:** `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
**Success gradient:** `linear-gradient(135deg, #a5d6a7 0%, #66bb6a 100%)`
**Error gradient:** `linear-gradient(135deg, #ffcdd2 0%, #ef9a9a 100%)`
**Level indicator:** `linear-gradient(135deg, #fbbf24, #f59e0b)`

### Обязательные классы для кнопок

```css
.back-button {
  background: linear-gradient(135deg, #667eea, #764ba2);
  color: white;
  border: none;
  border-radius: 20px;
  padding: 8px 16px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 3px 8px rgba(102, 126, 234, 0.3);
}

.back-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 5px 12px rgba(102, 126, 234, 0.4);
}

.level-indicator {
  background: linear-gradient(135deg, #fbbf24, #f59e0b);
  color: white;
  padding: 6px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 600;
  box-shadow: 0 2px 4px rgba(251, 191, 36, 0.3);
}
```

### Адаптивность (Mobile-First)

**Базовые размеры:**
- Минимальная ширина: 320px
- Точки перехода: 480px, 360px

**Использовать clamp():**
```css
font-size: clamp(14px, 3vw, 18px);
padding: clamp(12px, 3vw, 16px);
```

**НЕ использовать CSS переменные** - в проекте они не определены!
```

### 3. TypeScript Requirements

```markdown
## NFR-XXX: TypeScript Requirements

### Экспорт интерфейсов компонентов

**ПРАВИЛО:** Все интерфейсы Props в компонентах ДОЛЖНЫ быть экспортированы.

Для display/common компонентов (с `<script setup>`):

❌ **НЕПРАВИЛЬНО:**
```vue
<script setup lang="ts">
interface Props {  // приватный!
  items: string[];
}
</script>
```

✅ **ПРАВИЛЬНО:**
```vue
<script setup lang="ts">
export interface Props {  // публичный!
  items: string[];
}
</script>
```

### Интерфейсы в Options API (View-компоненты)

**ПРАВИЛО:** Интерфейсы, используемые в template, ДОЛЖНЫ быть экспортированы на верхнем уровне.

❌ **НЕПРАВИЛЬНО:**
```vue
<script lang="ts">
export default {
  setup() {
    interface Feedback {  // приватный!
      type: 'error' | 'success';
      message: string;
    }
    const feedback = ref<Feedback | null>(null);
    return { feedback };
  }
}
</script>
```

✅ **ПРАВИЛЬНО:**
```vue
<script lang="ts">
export interface Feedback {  // публичный!
  type: 'error' | 'success';
  message: string;
}

export default {
  setup() {
    const feedback = ref<Feedback | null>(null);
    return { feedback };
  }
}
</script>
```
```

### 4. Component Specifications

```markdown
## NFR-008: Component Specifications

### Структура компонента упражнения

**Имя:** [ExerciseName]Display.vue
**Путь:** `src/components/[exerciseName]/[ExerciseName]Display.vue`

**Обязательные props:**
```typescript
export interface Props {
  // Основные данные упражнения
  [exerciseSpecific]: number | string | boolean;

  // Опциональные UI props
  showResult?: boolean;
  showHint?: boolean;
}
```

**Правила:**
1. Использовать `<script setup lang="ts">` для display-компонентов
2. Экспортировать интерфейс Props
3. Использовать scoped styles
4. НЕ дублировать стили из common компонентов
```

### 5. Project Structure Requirements

```markdown
## NFR-009: Project Structure

### Добавление типов в src/types/index.ts

Каждое новое упражнение ДОЛЖНО добавить свой тип проблемы:

```typescript
// Добавить к существующим MathProblem интерфейсам
export interface NewExerciseProblem extends MathProblem {
  // Уникальные поля для упражнения
  exerciseSpecificField: number;
  anotherField: string;
  // ... другие поля
}
```

### Добавление роутов в src/router/index.ts

Добавить три роута по аналогии с column subtraction:

```typescript
{
  path: '/new-exercise/learning',
  name: 'newExerciseLearning',
  component: () => import('@/views/NewExerciseLearningView.vue'),
  meta: { requiresAuth: false }
},
{
  path: '/new-exercise/diagnostic',
  name: 'newExerciseDiagnostic',
  component: () => import('@/views/NewExerciseDiagnosticView.vue'),
  meta: { requiresAuth: false }
},
{
  path: '/new-exercise',
  name: 'newExercise',
  component: () => import('@/views/NewExerciseView.vue'),
  meta: { requiresAuth: false }
}
```

### Добавление полей в store (src/store/scores.ts)

```typescript
// Добавить к существующим полям
export const useScoresStore = defineStore('scores', {
  state: () => ({
    // ... существующие поля
    newExerciseScore: 0,
    newExerciseLearningCompleted: false,
    newExerciseDiagnosticPassed: false,
  })),
  actions: {
    updateNewExerciseScore(points: number) {
      this.newExerciseScore += points;
      this.saveScores();
    },
    // ... другие действия
  }
});
```
```

### 6. User Flow Requirements

```markdown
## NFR-010: User Flow

### Обязательный поток пользователя для нового упражнения

```
                    ┌─────────────────┐
                    │   Главная page  │
                    │   (HomeView)    │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   Обучение      │◄─────────────────┐
                    │   (Learning)    │                  │
                    └────────┬────────┘                  │
                             │                           │
                             ▼                           │
                    ┌─────────────────┐                  │
                    │   Диагностика   │                  │
                    │   (Diagnostic)  │                  │
                    └────────┬────────┘                  │
                             │                           │
                    ┌────────▼────────┐                  │
                    │   Пройдена?     │                  │
                    └────────┬────────┘                  │
                      │             │                    │
                 Да   │             │   Нет              │
                      ▼             ▼                    │
              ┌──────────────┐  ┌──────────────┐        │
              │  Тренировка  │  │   Результат  │        │
              │  (Training)  │  │   (fail)     │        │
              └──────────────┘  └──────────────┘        │
                      │                                     │
                      │                                     │
                      └─────────────────────────────────────┘
```

**Правила:**
1. Обучение обязательно перед диагностикой
2. Диагностика проверяет готовность (порог: 5/10)
3. При неудаче предложить повторить обучение
4. Тренировка доступна только после пройденной диагностики
```

### 7. Файловая структура

```markdown
## NFR-011: File Structure

### Обязательная структура файлов для нового упражнения

```
src/
├── components/newExercise/
│   ├── NewExerciseDisplay.vue          # Отображение примера
│   ├── NewExerciseVisualization.vue    # Визуализация (опционально)
│   └── (другие специфичные компоненты)
│
├── utils/math/newExercise/
│   ├── index.ts                        # Генератор задач
│   ├── __tests__/
│   │   └── index.test.ts               # Unit тесты
│   └── (другие utility модули)
│
├── views/
│   ├── NewExerciseLearningView.vue     # Обучение
│   ├── NewExerciseDiagnosticView.vue   # Диагностика
│   └── NewExerciseView.vue             # Тренировка
│
├── types/
│   └── index.ts                        # Добавить NewExerciseProblem
│
├── store/
│   └── scores.ts                       # Добавить поля очков
│
└── router/
    └── index.ts                        # Добавить 3 роута
```

**Правила:**
1. Компоненты в `components/` используют `<script setup>`
2. Views в `views/` используют Options API
3. Утилиты в `utils/math/` с тестами в `__tests__/`
4. Типы добавляются в существующий `types/index.ts`
```

### 8. Требования к тестированию

```markdown
## NFR-012: Testing Requirements

### Обязательные типы тестов для генератора задач

Каждый новый генератор задач ДОЛЖЕН иметь:

#### 1. Unit тесты
```typescript
describe('generateNewExerciseProblem', () => {
  it('генерирует корректную задачу');
  it('генерирует 4 варианта ответа');
  it('правильный ответ находится в вариантах');
});
```

#### 2. Property-based тесты (инварианты)
```typescript
describe('Инварианты', () => {
  it.each(testCases)('результат всегда положительный');
  it.each(testCases)('результат меньше 100');
});
```

#### 3. Edge cases тесты
```typescript
describe('Edge cases', () => {
  it('работает с минимальными значениями');
  it('работает с максимальными значениями');
  it('обрабатывает специальные случаи');
});
```

### Покрытие (Coverage)

- **Минимум:** 80% lines, 75% branches
- **Рекомендуется:** 90%+ lines, 85%+ branches
- **Функции:** 100%

### Команды тестирования

```bash
# Запустить все тесты
npm test -- --run

# Запустить тесты конкретного упражнения
npm test -- newExercise --run

# Запустить с coverage
npm test -- --coverage

# Смотреть режим
npm test -- newExercise
```
```

---

## 📋 Чек-лист перед созданием PRD

Используйте этот чек-лист перед началом написания PRD:

### Исследование (Research)

- [ ] Изучено **минимум 2** существующих упражнения (DecompositionView, MultiplicationView)
- [ ] Понята структура `app-container` → `game-container`
- [ ] Изучены все общие компоненты в `src/components/common/`
- [ ] Изучены все composables в `src/composables/`
- [ ] Понята система очков и валюты
- [ ] Изучена система маршрутизации
- [ ] Изучен `src/assets/css/main.css` — существующие глобальные стили
- [ ] Проверено, что ваши стили НЕ дублируют main.css

### Планирование (Planning)

- [ ] Определены уникальные компоненты (специфичные для упражнения)
- [ ] Определены переиспользуемые компоненты (из common/)
- [ ] Составлена карта переходов между экранами (обучение → диагностика → тренировка)
- [ ] Определены состояния сохранения (localStorage)
- [ ] Определено, какие CSS классы будут переиспользованы из main.css

### Документирование (Documentation)

- [ ] Добавлен раздел "Existing Patterns Reference" с конкретными путями
- [ ] Добавлены ссылки на файлы-примеры с номерами строк
- [ ] Добавлена таблица общих компонентов с props
- [ ] Добавлена обязательная структура DOM
- [ ] Добавлены требования к TypeScript (экспорт интерфейсов)
- [ ] Добавлен раздел "НЕ дублировать CSS" с примерами
- [ ] Добавлен чек-лист соответствия дизайну

### Соответствие дизайну (Design Compliance)

**Перед сабмитом на review проверить:**

- [ ] View использует `app-container` → `game-container` → `game-container-inner` структуру
- [ ] Импортированы ВСЕ общие компоненты из `src/components/common/`
- [ ] Кнопка "Назад" использует глобальный класс `.back-button`, НЕ дублирует стили
- [ ] `.level-indicator` использует глобальный класс из main.css, НЕ дублирует стили
- [ ] `.title` использует глобальный класс из main.css, НЕ дублирует стили
- [ ] В scoped styles только уникальные для упражнения стили
- [ ] НЕ используются CSS переменные (их нет в проекте)
- [ ] Используются фиксированные градиенты из Design System
- [ ] Адаптивность через clamp() как в существующих компонентах

---

## 🚀 Краткое содержание (Executive Summary для разработчика)

**Чтобы новое упражнение соответствовало архитектуре MathTrainer:**

1. **Импортируйте, не создавайте заново:**
   - GameOver, CoinAnimation, ScoreDisplay, ProgressBar, StarRating, AnswerOptions, CurrencyDisplay

2. **Следуйте структуре DecompositionView.vue:**
   - app-container → game-container → game-container-inner
   - Header с back-button, level-indicator, CurrencyDisplay
   - ScoreDisplay, ProgressBar, StarRating в указанном порядке
   - AnswerOptions в конце

3. **Используйте Options API для View-компонентов:**
   - Не `<script setup>`, а `export default { setup() { ... } }`

4. **Экспортируйте все интерфейсы:**
   - `export interface Props` в компонентах
   - Интерфейсы используемые в template должны быть публичными

5. **НЕ дублируйте CSS из main.css:**
   - `.back-button`, `.level-indicator`, `.title`, `.app-container`, `.game-container` — УЖЕ ЕСТЬ!
   - В scoped styles только уникальные стили упражнения
   - Проверьте `src/assets/css/main.css` перед написанием новых стилей

6. **Не используйте CSS переменные:**
   - Используйте фиксированные значения и градиенты из Design System

7. **Проверяйте по чек-листу:**
   - Каждая новая feature должна проходить Design Compliance Checklist

---

## 🔍 Дополнительные ресурсы

### Файлы для изучения перед началом

| Файл | Что изучить |
|------|------------|
| `src/views/DecompositionView.vue` | Полная структура View |
| `src/views/MultiplicationView.vue` | Работа с уровнями |
| `src/components/common/GameOver.vue` | Структура общего компонента |
| `src/components/common/AnswerOptions.vue` | Работа с вариантами ответов |
| `src/composables/useGameLogic.ts` | Управление состоянием игры |
| `src/composables/useCoins.ts` | Интеграция с валютой |
| `src/store/scores.ts` | Шаблон для добавления новых очков |
| `src/assets/css/main.css` | **Глобальные стили — НЕ дублировать!** |

### Полезные команды

```bash
# Проверить TypeScript компиляцию
npm run build

# Запустить dev сервер
npm run dev

# Запустить тесты (проект использует vitest)
npm test -- --run
# или
npm run test

# Проверить конкретные файлы
npx vue-tsc --noEmit src/views/NewExerciseView.vue

# Проверить дубликаты CSS в компонентах
grep -r "\.back-button" src/views/ --include="*.vue"
```

---

## История изменений

| Версия | Дата | Изменение | Автор |
|--------|------|-----------|-------|
| 1.0 | 2026-02-09 | Первая версия | Claude (на основе опыта column subtraction) |
| 1.1 | 2026-02-09 | Добавлен раздел "НЕ дублировать CSS" | Claude (после анализа main.css и дублирования в компонентах) |
